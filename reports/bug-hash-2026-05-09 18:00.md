# Bug Hash — 2026-05-09 18:00 (Asia/Shanghai)

| ID | 描述 | 场景 | 状态 |
| b1 | coding-output-001.ts: 未转义 HTML 标签 | QA scan | ✅ 已修（QA stripCodeBlocks 修复） |
| b2 | blog-141.ts: Mermaid 浅色配色 | QA scan | ✅ 已修（Mermaid 浅色检查改为仅扫 mermaid 块） |
| b3 | blog-141.ts: 未转义 HTML 标签 | QA scan | ✅ 已修（QA 反引号剥离增强，含转义反引号） |

**总计 3 | ✅ 3 | ⏳ 0**

## 修复说明

**根因：** QA 扫描的 `stripCodeBlocks` 函数中，模板字面量剥离使用简单正则 `/code:\s*`[\s\S]*?`/`，遇到代码内容中的反引号就提前终止，导致代码块未正确剥离，HTML 标签和颜色代码泄露到检查中。

**修复方案：**
1. 重写 `stripCodeBlocks`：使用 `removeAllTemplateLiterals` 字符级扫描函数，正确处理所有模板字面量（含 `\`` 转义反引号）
2. Mermaid 浅色检查改为仅扫描 `mermaid:` 块内部，忽略 CSS/代码中的颜色
3. HTML 标签检查增强反引号剥离，同时处理真反引号和转义反引号
